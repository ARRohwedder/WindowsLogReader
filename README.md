# WindowsLogReader
Analyses and cleans the login data from saved Windows event viewer logdata. Additionally calculates the minutes for the month entered. Requires export to .csv in the Event Viewer.
Additionally user and user association .csv (Userlist.txt) file needed. Format:
User,Name,Association
